# Рефакторинг кода

## Цель
Повышение читаемости, поддерживаемости и производительности.

## Найденные «запахи кода» и исправления

### 1. Дублирование кода (Repeated Code)
**Где**: Во всех эндпоинтах повторялись строки:
```python
conn = get_db_connection()
cursor = conn.cursor()
# ... запросы ...
conn.close()